/* eslint-disable no-undef */

import { expect } from 'chai';

describe('Food tests', () => {
  describe('createFood test', () => {
    it('should create food', () => {
      expect(1).to.equal(1);
    });
  });
});
